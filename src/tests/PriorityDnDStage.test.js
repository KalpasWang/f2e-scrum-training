import { render } from '@testing-library/react';
import { PriorityDnDStage } from '../components/Stages';

describe('PriorityDnDStage', () => {
  describe('當重新開始遊戲', () => {
    it('DnD 重置回預設的樣子', () => {
      // arrange
      render(<PriorityDnDStage />);

      // act
    });
  });
});
